{
  "name": "84dd",
  "version": "1.0.0",
  "description": "子非鱼，欢迎食用",
  "main": "index.js",
  "dependencies": {
    "@vuepress/plugin-back-to-top": "^1.0.0-rc.1",
    "@vuepress/plugin-pwa": "^1.0.0-rc.1",
    "moment": "^2.24.0",
    "vuepress": "^1.0.3"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1"
  },
  "scripts": {
    "dev": "vuepress dev .",
    "build": "vuepress build .",
    "deploy": "gh-pages -d dist",
    "deploy:build": "npm run build && gh-pages -d dist"
  }
}
